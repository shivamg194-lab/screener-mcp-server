services:
  - type: web
    name: screener-mcp-server
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: TRANSPORT
        value: http
      - key: NODE_VERSION
        value: 20.11.0
